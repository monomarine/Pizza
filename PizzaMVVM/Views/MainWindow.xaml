<Window x:Class="PizzaMVVM.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PizzaMVVM"
        xmlns:v="clr-namespace:PizzaMVVM.Views"
        xmlns:vm="clr-namespace:PizzaMVVM.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:CustomersViewModel}">
            <v:CustomersList/>
        </DataTemplate>
    </Window.Resources>

    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width ="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Клиенты"
                    Command="{Binding NavigationCommand}"
                    CommandParameter="customers"
                    Grid.Column="0"/>
            <Button Content="зарезервировано"
                    Grid.Column="1"/>
        </Grid>

        <StackPanel Grid.Row="1">
            <ContentControl Content="{Binding CurrentViewiModel}"/>
        </StackPanel>
    </Grid>
</Window>
